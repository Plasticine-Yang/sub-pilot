# This directory holds bundled OpenAI Whisper PyTorch models (see ADR-0005).
# Model files are git-ignored; fetch the default with:
#   ./scripts/setup-resources.sh
# Expected file: base.pt  (default base model, ~145MB)
# Larger models (small/medium/large-v3) are downloaded on demand in-app.
